# Pc-Cheak Node.js API

```bash
npm install
cp .env.example .env
npm start
```

Set `CORS_ORIGINS` to the exact GitHub Pages origin and use HTTPS in production.
Use persistent storage for `data/` so reports and activation keys survive restarts/redeploys.
